en:
  activemodel:
    errors:
      models:
        upload:
          attributes:
            file:
              must_be_valid_csv_file: is malformed, not a valid csv file
              must_have_data_rows: is empty, must have rows with data
              must_have_required_headers: must have name and password headers
